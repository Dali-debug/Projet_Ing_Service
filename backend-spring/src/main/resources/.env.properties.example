# spring-dotenv prefix configuration
# This tells spring-dotenv to expose .env vars with the "env." prefix,
# matching the ${env.VAR_NAME} placeholders in application.properties.
# Remove this file and update application.properties if you drop the prefix.
prefix=env.
